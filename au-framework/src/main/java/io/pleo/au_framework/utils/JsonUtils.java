package io.pleo.au_framework.utils;

import io.pleo.au_framework.data.JsonConfig;

/** A utility class that provides common methods that can be used when working with JSON files. */
public class JsonUtils {

  public static JsonConfig readJson(String fileName) {
    // TODO: Should we enforce the extension to be passed here or handle it?

    // TODO: handle case-sensitivity with the Platform and ExecutionMode enums.

    return null;
  }
}
